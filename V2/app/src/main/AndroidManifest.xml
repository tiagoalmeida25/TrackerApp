<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.tiago.trackerapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.TrackerApp">
        <activity
            android:name=".v3.MainMenu"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".v3.SliderActivity">
        </activity>
        <activity
            android:name=".v3.CounterActivity">
        </activity>
        <activity
            android:name=".v3.NumericalActivity">
        </activity>
        <activity
            android:name=".v3.TextActivity">
        </activity>
        <activity
            android:name=".PreferenceUtils"
            android:label="@string/title_activity_preference_utils" />
        <activity
            android:name=".Constants"
            android:label="@string/title_activity_constants" />
        <activity
            android:name=".Item"
            android:label="@string/title_activity_item" />
        <activity
            android:name=".Edit"
            android:label="@string/title_activity_edit" />
    </application>

</manifest>